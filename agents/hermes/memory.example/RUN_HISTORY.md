# Run History

## 1970-01-01T00:00:00Z — Seed example

Autonomy level: L1
Approval required: none

Results:
- This is an example memory seed. Copy `memory.example/` to `memory/` (gitignored)
  to start a live Hermes memory.
- Each real planning cycle appends a dated section here automatically.

Next action: run `python -m hermes.run --objective "..."` to record the first cycle.
